# devops-ai — Evolutions Backlog

*A running log of framework evolutions we intend to make. Each entry states the problem before the solution, so entries stay meaningful over time.*

## 1. Human–model contract rewrite (in design)

**Problem:** the kdesign/kplan/kbuild framework prescribes process, which caps frontier models at the human's plan and wastes their planning/verification abilities.
**Direction:** outcome-only intent specs and work briefs, executor agency with an escape valve, anti-deference escalation protocol, comprehension layer (glossary, architecture-as-tests, event-triggered interrogation).
**Status:** v6 piloted end to end on khealth "challenges" (2026-09-01 → 09-11, `docs/designs/v2-contract/PILOT.md`); synthesised in `REVIEW.md`; contract, rules, skills and templates revised to v7 (September 2026). Next: a second pilot on a different product, watching what v7 introduced.

## 2. Conformance & e2e validation integration

**Problem:** the new Fable conformance review covers intent conformance and architectural coherence only; functional e2e validation (as devops-ai does today) must stay, and the two need a clean seam.
**Direction:** define how the intent-level review and the e2e pipeline compose — ordering, shared artifacts (briefs' acceptance criteria as e2e seeds?), what blocks a feature boundary.
**Status:** identified during contract-rewrite review; deliberately kept out of the contract doc's scope.

## 3. Code review process ("babysitting PRs")

**Problem:** PR review currently demands ongoing human attention, and per the new contract, human PR-reading neither catches the failures that matter (cross-PR inconsistency) nor is a good use of energy.
**Direction:** to be designed — likely builds on the Pi validator-agent architecture and the conformance review, with the human involved only at escalation.
**Status:** first concrete step landed 2026-09-12 (devops-ai #34, data in #33): the babysit loop now requires a written `## Review scope` in the PR body, kreview has an OUT OF SCOPE disposition (true but not this PR's → issue), every finding carries a computed provenance (original diff / review-fix commit / unknown; the boundary is the earliest review still in the branch's history, so a rebase resets it at the cost of at most one extra round), and a round whose findings all sit on fix commits is the last one. Six criteria signed by Karl item by item; the pilot data (13- and 11-round loops) is in #33. Also landed 2026-09-12 (#25): the loop must run **on an Opus-grade model**, never on whatever tier the invoking session happens to be. #45 enforced that by forking — `skills/kbabysit/SKILL.md` frontmatter carried `context: fork`, `agent: general-purpose`, `background: false` and `model: claude-opus-5` ([skill frontmatter reference](https://code.claude.com/docs/en/skills.md)) — for tier economics (polling plus bounded judgement doesn't warrant top-tier tokens) and context hygiene (poll/CI/review output stays out of the invoking session). **The fork was dropped on 2026-09-14 (#75)**: it fixed the tier and hid the loop, leaving the session that ran a babysit showing a status line and nothing else, with every round's reasoning in a sidechain transcript nobody opens (measured on #72 and #66 — two full babysits, two empty panes). The four frontmatter fields are gone. The tier is now the session's own: a babysit runs in an Opus agent-deck session created from the PR's worktree, and the skill's preflight states the model as a `MODEL:` line and stops the run on anything but an Opus-grade one, with `tests/architecture/test_v2_contract.py` pinning both halves. Context hygiene is bought the same way — that session has no other job — rather than by a subagent boundary. The skill body still carries `$ARGUMENTS` and resolves the PR from the checkout, since the session sees no planner conversation either; `kreview` runs in that same session, unforked, so each round's reasoning stays where the loop's decisions are made. The human-at-escalation design above is still open.

Second measurement, 2026-09-13 (devops-ai #61, from the `secret-providers` M2 and M3 PRs — #51 is M2, OpenBao; #49 is M3, Azure Key Vault): #49 and #51 drew **13 and 12 Copilot reviews**, and **20 of those 25 were spent outside the loop**. Both PRs stopped correctly *under* `kbabysit` — #49 on budget after 3 rounds, #51 second-order after 2, reports posted — and then, nine hours later, the observer seat ran a hand-rolled loop: it re-requested Copilot itself each round and relayed pre-decided dispositions to the executors (`(1) FIX … (2) IMPLEMENT …`), so no triage, no provenance, no scope judgement and no budget applied to any of the 20. Two content failures compounded it: rounds 6–11 of #49 were five correct patches to five echo sites of **one** root cause (unvalidated `akv://` segments echoed into `az` error text), filed as a class only afterwards, as #60; and only **4 of the 13** Copilot reviews on #49 opened a thread at all — from round 6 (14:27) on, 7 of the last 8 opened none, so 8 of the 9 findings that phase produced were *suppressed comments* living only in review bodies, invisible to a triage that reads threads. Counting them fires the existing second-order stop at round 6: that round's only two findings, `azurekeyvault.py:245` and `:160`, blame at the review's own commit (`70a8f107`) to `23ee88a2` and `63465ed7`, both descendants of the boundary — seven paid rounds after the loop already had the rule to stop. Six mechanisms landed from the signed list: (1) every finding carries `isolated | systemic → <root cause>` before it gets a disposition, and a `systemic` root cause on pinned Surface stops the loop as the human's decision rather than buying the next round; (2) `kselfreview` asks the same question over its own findings and reports a shared mechanism once, not per site; (3) the observer seat has **no re-request verb** and relays no dispositions — `/kbabysit <n>` or a question to the human, nothing else; (4) the first pass should run at Copilot's deeper effort level, which turned out **not** to be request-time — "Review effort level" (`Lite`/`Balanced`) is a repository/organization setting at Settings → Copilot → Code review, with no REST, GraphQL or ruleset parameter, so `kbabysit` records the level per round and names the setting for the human instead of changing it; (5) suppressed comments are parsed out of review bodies, given computed provenance from the review's own `commit_id`, and counted in the second-order stop; (6) **re-entry**: any round after a posted report, from any seat and for any reason, goes through `/kbabysit <n>`, which re-applies the budget and stop rules from that report onward — new commits are a legitimate trigger, but the trigger re-enters the loop, it never bypasses it.

Third step, signed 2026-09-13: the `review-loop-runtime` feature (`docs/specs/review-loop-runtime/SPEC.md`, PR #66) moves the deterministic half of the loop out of the two skills and into a `kreview` console script — fetch, suppressed-comment parsing, provenance, the stop rules as a decision table, the loop's state as a block in the PR's babysit comment — so a round's model work is judgement only and no signed rule depends on a model remembering it. It changes *how* the babysit loop runs, not who reviews or what a disposition means; CONTRACT.md's "the product's code review process, exactly as any PR is today" is unaffected, and this backlog item stays the place that evolution is recorded (AGENTS.md: a change to the contract's mechanics belongs here or in CONTRACT.md, never only in a skill). **It also reverses the forked-Opus mechanism recorded above**, decided by Karl 2026-09-14: M2 drops `context: fork` and `model:` from `skills/kbabysit/SKILL.md`, because the fork hid every round in a sidechain transcript (measured on #72 and #66) — the tier economics and context-hygiene reasons the 2026-09-12 entry gives were outweighed by a human who could not read what the loop did. The Opus-grade requirement itself is unchanged; what moves is how it is enforced — the loop runs in the Opus agent-deck session that invokes it, and `kbabysit`'s preflight states the model and stops on the wrong one, with `tests/architecture/test_v2_contract.py::test_babysit_loop_is_pinned_to_a_forked_opus_subagent` retargeted from the frontmatter pin onto that check. The paragraph above is left standing as the record of what was true between 2026-09-12 and 2026-09-14 rather than edited, since this file is the durable history.

Third measurement, 2026-09-14 (devops-ai #70): the observer seat told the human a PR was **merge-ready** while the newest section of its babysit report read `⚠️ needs human decision — systemic root cause on pinned Surface`, the CodeQL check was red, and two of the three open items were undecided. Karl's words: a massive trust issue. The failure is not in the loop — `kbabysit` had stopped correctly and written the right verdict — but in the *relay*: the seat that carries a verdict to the human restated it instead of quoting it, and the restatement was an upgrade. So the verdict is now contract, not summary. `kobserve verify` takes as its `In:` condition the **newest section**'s `**Verdict:** ✅ merge-ready` line; any other verdict (⚠️ or ❌) makes the seat's whole move quoting that line and its `**Why the loop stopped:**` line verbatim, and the word "merge-ready" is one the seat repeats from the report rather than coins. A `DISCUSS` item the human has since decided does not change a verdict in chat — it is closed by the executor's `/kbabysit <n>` re-entry, which appends a section carrying the new verdict, so the verdict only ever moves where the stop rules apply. This is mechanism (3) of the #61 list — the observer seat's loss of its re-request verb — extended from *what the seat may do* to *what the seat may say*: both failures were the seat substituting its own judgement for the loop's, once by re-requesting and once by re-wording. The architecture test pins the guardrail and the `## verify` gate that carries it.

Fourth measurement, 2026-09-20 (devops-ai #66): six paid rounds, of which the loop stopped **four times on rules that ended nothing** — each stop a re-entry Karl had to word by hand, and none of them a statement that the reviewer was done. The default three-round budget fired on a round that had converged anyway; the second-occurrence systemic rule fired on a class fix that was working; a DISCUSS blocked merge-readiness over a question the seat was free to answer itself. Karl's decision: *"if it's not converged — and not diverging — we don't stop."* A decision that is his is a **wait**, not a stop, and everything a seat may decide, it decides. So `kbabysit` step 4 now has exactly two non-convergence families and no default budget at all. **Diverging** — a root cause back a **third** time (the second occurrence is a wider class fix and a sentence in the report, never a stop); **no progress**, meaning the count of findings on the original diff failed to fall twice in a row — three consecutive rounds, each with at least one, the second not fewer than the first and the third not fewer than the second, so a first round has nothing to fall from and this can never end a loop at round 2 (pinned 2026-09-21 in `0b136c9` after #85's own loop read the first wording both ways; M2's blocking test `test_apply_no_progress_diverges` pins the same three rounds); and **oscillation**, the same reviewer suggesting X then reverting X. **Waiting on the human** — a `systemic` root cause on pinned Surface, where "pinned" is now defined per seat as *Surface this seat may not change* (for an executor the brief it builds against; for a planner on its own spec PR, only a decision the human signed — a gap in a Surface the same seat wrote is that seat's to fix); an open DISCUSS, which is only ever a decision the human owns; an explicit `max-rounds:` the human states for that run; and red CI. This **supersedes the round-budget clause of mechanism (6) above** — re-entry re-applies the stop rules, and there is no budget for it to re-apply unless the human states one — and the "on budget after 3 rounds" outcome recorded for #49 in the second measurement is history, not a rule that still exists. The convergence family is unchanged: second-order round, no in-scope IMPLEMENT, no new findings, repeats-only. `skills/kbabysit/SKILL.md` carries this as version 0.6.0. The `review-loop-runtime` spec's M2 brief (#66, still open) carries the same **decision** for the tool that is to replace the prose — the three stop kinds `converged` / `diverging` / `escalate`, no default budget, and a second occurrence of a root cause explicitly not a stop — but not the same table: its rows route oscillation and CI through the generic `--stop REASON` escalate path rather than giving them rows of their own, so the prose here is finer-grained than the tool will be until M2 lands. This entry is the contract-level record while the two coexist, and the skill is the operative one for sessions running today.

## 4. Roadmap representation & grounding

**Problem:** running multiple teams of agents in parallel creates a heavy context-switch tax; the human needs a way to ground quickly at any moment — where each product stands, what's on deck short-term and long-term.
**Direction:** to be designed — a maintained, always-current roadmap view aggregating feature specs' status across products; possibly generated from the specs themselves so it can't drift from reality.
**Status:** not started.

## 5. CI budget and gate events

**Problem:** the always-run CI job must stay under 2 minutes (hard limit; ideally under 1) — the human's principle, and why it runs unit tests only. The milestone blocking gate is E2E and cannot live there; running it on every push would also be overly expensive.
**Direction:** blocking acceptance tests run as a separate, selectively-triggered workflow — on PR ready-for-review plus a manual re-trigger, required at merge — never in the standing `check` job. Needs per-project infra answers (can this project's stack stand up in a runner at all?).
**Status:** principle agreed 2026-08-30; wiring deferred to its own feature (see roadmap "PR gate wiring").

## 6. kinfra beyond Python containers

**Problem:** kinfra grew up on Python apps in Docker; the quality generation (uv-flavored Makefile, pytest conftest guardrails) is Python-centric with a thinner Node path, and the human is expanding beyond that world.
**Direction:** to be designed — keep the language-agnostic core (worktrees, slots, ports, compose, observability, guards) and make the quality layer pluggable per stack rather than grown by special cases.
**Status:** identified 2026-08-30, not started.
